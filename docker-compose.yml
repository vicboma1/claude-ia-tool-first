version: "3.9"

services:
  app:
    image: maven:3.9.6-eclipse-temurin-21
    container_name: java-maven-app
    working_dir: /app

    volumes:
      - ./:/app
      - m2-cache:/root/.m2

volumes:
  m2-cache: